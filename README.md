# tubes-praktikum-alpro
Buat py nya, bikin virtual env dulu kalau pake cmd `python -m venv .venv` <br />
Habis itu aktivasi dulu kalau cmd `.venv\Scripts\activate` kalau powershell `.\.venv\Scripts\Activate.ps1` <br />
Beres itu baru install library nya <br />
`pip install -r requirements.txt` <br />
Nanti ke install semua library2 yang di butuhin